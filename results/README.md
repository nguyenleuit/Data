# Results

Benchmark results should be stored under `runs/` with one directory per framework/model/config execution.

Recommended layout:

```text
runs/<timestamp>_<framework>_<model>/
  config.yml
  summary.json
  cases/
  transcripts/
  logs/
```
