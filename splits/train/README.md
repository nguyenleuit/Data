# Train Split

This split is reserved for framework development, prompt tuning, debugging, and training-oriented cases.

The current train split contains 40 runnable CVE labs listed in `CVE_train.md`. Train cases may expose development material such as hints, writeups, solution traces, and solution scripts. Do not copy held-out test CVEs into this split unless the split policy is explicitly changed.
