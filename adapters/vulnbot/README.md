# VulnBot Adapter

This adapter will translate dataset cases into VulnBot target descriptions and runtime config.

The active implementation is pending because VulnBot requires runtime Kali/SSH and database configuration.
