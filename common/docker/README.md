# Common Docker Assets

This directory contains reusable Docker Compose snippets for rebuilt cases.

The snippets are intentionally generic. Individual cases may include or copy them when their runtime is rebuilt.
