# Common Secrets

This directory will hold deterministic secret generation assets for rebuilt labs.

Secrets should be generated per run or per case from a controlled seed, and they must not be exposed through public task files.
